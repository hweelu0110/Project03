<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">
	<resultMap type="memberDTO" id="memResult">		
		<result property="email" column="MEM_ID" />		
		<result property="pwd" column="MEM_PWD" />
		<result property="name" column="MEM_NAME" />
		<result property="gender" column="GENDER" />
		<result property="birth" column="BIRTH" />
		<result property="phone" column="PHONE" />
		<result property="img" column="IMG" />
		<result property="open" column="MEM_OPEN" />
		<result property="joinDate" column="JOINDATE" />
	</resultMap>

	
	<!-- 회원가입 -->
	<insert id="register">
		INSERT INTO EZEN.ALTO_MEMBER(MEM_ID, MEM_PWD, MEM_NAME, GENDER, BIRTH)
		VALUES(#{email}, #{pwd}, #{name}, #{gender}, #{birth})
	</insert>
	
	<select id="idCnt" parameterType="memberDTO" resultType="int">
		<![CDATA[
			select count(*) from ALTO_MEMBER where mem_id = #{id}
		]]>
	</select>
	
	<insert id="createAuthKey">
		insert into ALTO_MEMBER_AUTH values(#{memberEmail}, #{authKey})
	</insert>
	
	<update id="memberAuth">
		<![CDATA[
			update ALTO_MEMBER set AUTHKEY = 1 
			where (select count(*) from ALTO_MEMBER_AUTO where EMAIL = #{memberEmail}) > 0
		]]>
	</update>
															
	<!-- 로그인 -->
	<select id="login" resultMap="MemberDTO">
		select * from ALTO_MEMBER where MEM_ID = #{email}	
	</select>	
	
	
</mapper>